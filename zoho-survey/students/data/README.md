# students/data

⚠️ **Esta carpeta ya no contiene datos.**

Los archivos CSV se movieron a `../../data/` (raíz del proyecto `survey-storytelling/`) para servir como fuente única centralizada para todos los tipos de encuesta.

Ver `../README.md` para más detalles sobre el flujo de datos.
