sentence-transformers>=2.2.2
pandas>=1.5.0
scikit-learn>=1.0.0
spacy>=3.5.0
jsonschema>=4.0.0
# El modelo de spaCy debe instalarse explícitamente con:
#   python -m spacy download es_core_news_sm
# No se descarga automáticamente en runtime (Fase 6: eliminación de dependencias no determinísticas).

# ============================================================
# Fase IA — Análisis Cualitativo con DeepSeek
# ============================================================
# El cliente DeepSeek usa urllib (stdlib), NO requiere dependencias extra.
# Para activar el modo IA, definir la variable de entorno:
#   DEEPSEEK_API_KEY=sk-...   (en GitHub Actions Secrets)
# Si la key no está, el pipeline cae automáticamente al modo legacy
# (spaCy + sentence-transformers) sin cambios.
# Opcional: IA_CUALITATIVO_FALLBACK=1 fuerza el modo legacy incluso con key.
# Opcional: IA_CUALITATIVO_CACHE=0 desactiva el caché (default: activado).
