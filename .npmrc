# Use hoisted node_modules instead of symlinks
# Required because the project is on a network drive where symlinks are not supported
node-linker=hoisted

# Strict lockfile validation
strict-peer-dependencies=false

# Allow scripts execution
ignore-scripts=false
