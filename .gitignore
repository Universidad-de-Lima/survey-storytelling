# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE
.vscode/

# Playwright
test-results/
.idea/
*.sublime-*

# Datos fuente: los CSVs se commitean al repo (sanitizados automaticamente
# por zoho-survey/scripts/sanitize_csv_pii.py antes del procesamiento ETL).
# Las columnas PII (Direccion IP, Agente Usuario, URL de la encuesta) se
# redactan in-place, por lo que los CSVs en data/ no contienen PII.

# Artefactos binarios generados por ETL
*.zip

# Caché y logging de IA
ia_cache.json
.csv_hash
AI_STATE.md
AI_ACTION_LOG.md

# Tests
tests/screenshots/
tests/snapshots/

# Misc
Mejoras/
*.log
node_modules/

# Logs
*.log

# Node (para tests JS con jsdom)
node_modules/

# Cache de modelos (descargados en CI, no se commitean)
~/.cache/huggingface/
~/.cache/spacy/

testsprite_tests/tmp/config.json

# Archivos de planificación (solo locales, no en producción)
Mejoras/

# Archivos de caché y estado (artifacts, no en producción)
ia_cache.json
.csv_hash
*.cache

# Screenshots y snapshots de tests (solo locales)
tests/screenshots/
tests/snapshots/

# AI State y Action Log (locales de la IA, no en producción)
AI_STATE.md
AI_ACTION_LOG.md

# Archivos temporales de desarrollo
*.tmp
*.bak
*.orig
